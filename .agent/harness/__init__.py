"""Shared multi-agent harness: runs, controls, coordinates, and verifies agents.

Agent definitions/instructions live in .agent/agents/*.md, not here — this
package is deliberately agent-agnostic (see orchestrator.py).
"""
