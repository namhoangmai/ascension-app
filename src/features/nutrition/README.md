Phase 6 owns food search, gram-based macro calculations, food logs, saved meals, and nutrition goals.

FatSecret food imports go through the server-only `/api/nutrition/fatsecret/food` route. Set `FATSECRET_ACCESS_TOKEN` in the local environment before importing by food id.
